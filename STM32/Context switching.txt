.syntax unified
.cpu cortex-m3
.thumb
.global main
/* ------------------------------------------------------------
   🧠 Stack pointers (simulation only)
   দুইটা আলাদা টাস্কের জন্য আলাদা stack memory ধরা হলো
------------------------------------------------------------ */
.equ TASK1_STACK, 0x20001000
.equ TASK2_STACK, 0x20002000

/* ------------------------------------------------------------
   🧩 Pseudo tasks — বাইরে define করা হবে
------------------------------------------------------------ */
.extern task1
.extern task2


/* ------------------------------------------------------------
   🏁 প্রোগ্রাম শুরু
------------------------------------------------------------ */
main:
    LDR r0, =TASK1_STACK      @ Task1 এর stack pointer ঠিকানা
    LDR r1, =TASK2_STACK      @ Task2 এর stack pointer ঠিকানা

    MOV sp, r0                @ Stack pointer সেট করলাম Task1 এর জন্য
    BL task1                  @ Task1 শুরু


/* ------------------------------------------------------------
   🔁 Context Switch Routine
   👉 কাজ: Task1 ↔ Task2 এর মাঝে context বদলানো
------------------------------------------------------------ */
context_switch:
    /* 🧩 Step 1: বর্তমান Task এর রেজিস্টার Stack এ Save করা */
    PUSH {r4-r11, lr}         @ Low registers (callee-saved) সংরক্ষণ

    /* 🧩 Step 2: বর্তমান Task এর SP সংরক্ষণ */
    STR sp, [r0]              @ Task1 এর stack pointer মেমরিতে রাখো

    /* 🧩 Step 3: অন্য Task এর SP লোড */
    LDR sp, [r1]              @ Task2 এর stack pointer রিস্টোর করো

    /* 🧩 Step 4: Task2 এর রেজিস্টার পুনরুদ্ধার */
    POP {r4-r11, lr}          @ পূর্বের মানগুলো restore

    /* 🧩 Step 5: নতুন Task Resume */
    BX lr                     @ Link register এ ফিরে গিয়ে পরের Task চালাও

    B .                       @ এখানে থেমে থাকবে (infinite loop, debugging)


/* ------------------------------------------------------------
   🪄 Task 1: LED Blink Simulation
------------------------------------------------------------ */
.thumb_func
task1:
    MOV r4, #0                @ LED blink counter

loop1:
    ADD r4, r4, #1
    CMP r4, #1000
    BNE loop1                 @ Delay simulation
    BL context_switch         @ Task1 → Task2 context switch
    B task1                   @ আবার Task1 চালাও


/* ------------------------------------------------------------
   🪄 Task 2: Button Check Simulation
------------------------------------------------------------ */
.thumb_func
task2:
    MOV r5, #0                @ Button press counter

loop2:
    ADD r5, r5, #1
    CMP r5, #500
    BNE loop2                 @ Delay simulation
    BL context_switch         @ Task2 → Task1 context switch
    B task2                   @ আবার Task2 চালাও
